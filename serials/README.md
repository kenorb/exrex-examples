# Serial Generation Scripts

This folder contains scripts for generating various types of serials, such as PCI IDs and licenses.

## Usage

Each script uses `exrex` to generate the specified serial format.